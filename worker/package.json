{
  "name": "multiport-counter-api",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 execute multiport-counter-db --local --file=./schema.sql",
    "db:migrate:remote": "wrangler d1 execute multiport-counter-db --remote --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
